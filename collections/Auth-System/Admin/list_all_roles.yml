info:
  name: list_all_roles
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/admin/roles
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
