info:
  name: list_all_roles
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/admin/roles"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
