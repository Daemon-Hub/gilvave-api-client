info:
  name: list_all_permissions
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/admin/permissions"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
