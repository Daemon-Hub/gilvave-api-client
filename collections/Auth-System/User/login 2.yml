info:
  name: login 2
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_HTTP_URL}}/users/login"
  body:
    type: json
    data: "{\r

      \  \"email\": \"2@gmail.com\",\r

      \  \"password\": \"2\"\r

      }"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody();
        bru.setGlobalEnvVar('access_token2', response.access_token);
        bru.setGlobalEnvVar('refresh_token2', response.refresh_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
