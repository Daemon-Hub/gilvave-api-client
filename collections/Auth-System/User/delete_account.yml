info:
  name: delete_account
  type: http
  seq: 7

http:
  method: DELETE
  url: http://127.0.0.1:8000/users/me
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
