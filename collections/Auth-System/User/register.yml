info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_HTTP_URL}}/users/register"
  body:
    type: json
    data: |-
      {
        "username": "1",
        "email": "1",
        "password": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
