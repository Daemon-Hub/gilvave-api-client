info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_HTTP_URL}}/users/register"
  body:
    type: json
    data: |-
      {
        "username": "2",
        "email": "2@gmail.com",
        "password": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
