info:
  name: change_password
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{BASE_HTTP_URL}}/users/me/change-password"
  body:
    type: json
    data: "{\r

      \  \"current_password\": \"123123\",\r

      \  \"new_password\": \"1\",\r

      \  \"new_password_confirm\": \"1\"\r

      }"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
