info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8000/users/logout
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
