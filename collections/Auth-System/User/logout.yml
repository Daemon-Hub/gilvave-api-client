info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_HTTP_URL}}/users/logout"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
