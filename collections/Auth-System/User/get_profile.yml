info:
  name: get_profile
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/users/me"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
