info:
  name: ws1
  type: websocket
  seq: 1

websocket:
  url: "{{BASE_WS_URL}}"
  message:
    type: text
    data: "{{mc}}"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
