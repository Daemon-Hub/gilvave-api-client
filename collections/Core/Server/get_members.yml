info:
  name: get_members
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/servers/{{server_id}}/members"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
