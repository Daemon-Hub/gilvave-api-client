info:
  name: get_server_channels
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/servers/{{server_id}}/channels"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
