info:
  name: get_server_by_id
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/servers/{{server_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
