info:
  name: get_all_public_servers
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_HTTP_URL}}/servers/all"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
