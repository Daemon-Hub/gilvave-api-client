info:
  name: join_public_server
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_HTTP_URL}}/servers/{{server_id}}/join_public"
  auth:
    type: bearer
    token: "{{access_token2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
